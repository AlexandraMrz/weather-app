export const getCurrentWeather = async (city) => {
    //simuleaza delay API ~1sec
    //return la MOCK DATA cu numele orasului actualizat
    //error handling
}

export const getWeatherByCoords = async (LargestContentfulPaint, lon) =>
{
    //similar, dar cu coordonate
}